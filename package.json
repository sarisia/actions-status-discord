{
  "name": "actions-status-discord",
  "version": "1.0.0",
  "description": "Post Github Actions CI status to Discord",
  "main": "lib/main.js",
  "scripts": {
    "build": "npx tsc",
    "release": "npm install && npm run build && npm prune --production",
    "tag": "git tag -f v1 && git push origin :v1 && git push origin v1"
  },
  "author": "Sarisia",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.7.2",
    "@types/node": "^13.13.5"
  },
  "dependencies": {
    "@actions/core": "^1.2.0",
    "@actions/github": "^1.1.0",
    "axios": "^0.19.0"
  }
}
